Thinking of using Golden Count for the counting system

First on list: Get the AI set up
    - Get the ai to recognize cards
    - assign values to each card
    - get the ai to recognize all players 
    - calculate the running count
    - figure out how to run on computer 